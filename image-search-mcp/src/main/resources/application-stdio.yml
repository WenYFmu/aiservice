spring:
  ai:
    mcp:
      server:
        name: image-search-mcp-server
        version: 1.0.0
        type: sync
        stdio: true
  main:
    web-application-type: none #无需开启mvc服务
    banner-mode: off #关闭控制台输出，因为服务需要通过stdio来进行交互